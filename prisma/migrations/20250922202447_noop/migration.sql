-- NOOP migration to reconcile migration history
-- This migration exists in some environments' _prisma_migrations table
-- but was missing from the repository. Adding it prevents drift errors.

-- Intentionally empty (no SQL).

